# Welcome to Shop List API

## STEP FOR DEV

    - npm install
    - change name's file .env-example to .env and follow instruction inside
    - npm run dev
    - the port by default is localhost:3000
